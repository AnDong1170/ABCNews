package Util;

public class Constant {
    public static final String UPLOAD_DIRECTORY = "assets/uploads"; // thư mục lưu ảnh
    public static final String DEFAULT_FILENAME = "default.jpg";   // ảnh mặc định nếu không upload
    public static final long MAX_FILE_SIZE = 10 * 1024 * 1024;     // 10MB (tùy chỉnh thoải mái)
    public static final long MAX_REQUEST_SIZE = 50 * 1024 * 1024;  // 50MB cho toàn bộ request
}